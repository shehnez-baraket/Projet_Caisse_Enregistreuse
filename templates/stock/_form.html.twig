<style>

	.container {
		padding: 80px;

	}
	.form-group {
		margin-top: -5px;
		width: 40em;
	}
	.form-control {
		width: 70%;

	}
	.form-group1 {
		padding-left: 7em;
		color: Navy;

	}
	.btn-success {
		margin-left: 12em;

	}
</style>
<div style="width: 600px; ; padding-top:10px; padding-bottom:10px;border: 3px solid #A0A0A0;background: #C0C0C0; margin-left:24em;">

	<div class="container">
		{{ form_start(form) }}
		<div class="form-group">{{form_row(form.produit,{'attr':{'class':'form-control'}})}}</div>
		<div class="form-group">{{form_row(form.maxQte,{'attr':{'class':'form-control'}})}}</div>
		<div class="form-group">{{form_row(form.minQte,{'attr':{'class':'form-control'}})}}</div>
		<div class="form-group">{{form_row(form.Dispo,{'attr':{'class':'form-control'}})}}</div>
		{{ form_widget(form) }}
		<button class="btn btn-success">{{ button_label|default('Save') }}</button>
		{{ form_end(form) }}
	</div>
</div>
